interface:
  display_name: "Git Workflow"
  short_description: "Use for any git status, commit, branch, pull, or push work"
  default_prompt: "Use $git-workflow before any git-related request, including status, diff, log, branch changes, commits, pulls, pushes, or PR summaries."
